/* Repo path: src/data/scenes.js
 *
 * The two scenes. There is no third — each of the five tables opens an overlay panel
 * over `interior`, not a scene of its own (PRD R7/R11).
 *
 * Actor positions are percentages of the stage, so they hold at any viewport size.
 * `bottom` is measured from the stage floor, which is how a character standing on the
 * ground actually behaves when the stage changes height.
 */

import { sceneAlt, actorAlt, signs, decor } from './content.js';

/* Jingwen's three views. Each is cut into a body and two legs so the legs can move,
 * and all three are cut at the SAME percentages (see bodyHeight/legTop below) so the
 * hip geometry is identical and she cannot jump when she turns.
 *
 * `aspect` differs per pose because a profile is narrower than a front view; the
 * renderer updates the actor box when the pose changes. Heights are normalised, so
 * she stays the same height throughout. */
function pose(prefix, aspect) {
  return {
    aspect,
    body: `assets/sprites/${prefix}-body.png`,
    legLeft: `assets/sprites/${prefix}-leg-left.png`,
    legRight: `assets/sprites/${prefix}-leg-right.png`,
  };
}

const jingwenFront = pose('jingwen', 222 / 720);
const jingwenBack  = pose('jingwen-back', 227 / 720);
const jingwenSide  = pose('jingwen-side', 193 / 720);

export const scenes = {
  exterior: {
    id: 'exterior',
    image: 'assets/scenes/exterior.jpg',

    /* The painting is opaque edge to edge, which would hide the landmark backdrop
     * behind it completely. This mask punches the sky out so the backdrop shows
     * through — the same result as a transparent cutout, but the artwork stays a
     * 375 KB JPEG and the mask is 13 KB of almost-flat alpha. The equivalent RGBA
     * PNG was 996 KB, which would have blown the whole page budget on its own.
     *
     * Generated by keying the sky; the exterior regeneration replaces both. */
    mask: 'assets/scenes/exterior-mask.png',
    alt: sceneAlt.exterior,
    aspect: 1408 / 768,            // the artwork's shape; see engine/layout.js
    hasBackdrop: true,             // the landmark layer only appears outdoors

    /* Walkable ground, as a ring of [x, y] image coordinates (R4).
     *
     * Traced against the artwork rather than approximated, because a rectangle puts
     * her ankle-deep in the roses. What it deliberately excludes:
     *   - the rose and lavender bed filling the bottom-left corner   (x below ~25)
     *   - the stone-edged raised bed across the bakery front         (y above ~88)
     *   - the fountain and its base                                  (y above ~83)
     *   - the pink flowering bushes right of the fountain            (y above ~86)
     *   - the lavender along the bottom-right                        (x beyond ~84)
     * leaving the garden path, the doorstep, and the open lawn.
     *
     * To retune: run the grid overlay described in CLAUDE.md, or just walk her to
     * each corner in the browser and check her feet stay on painted ground. */
    walkable: [
      [37, 83],   // the doorstep -- the Phase 5 way in
      [48, 83],
      [57, 86],   // clear of the potted plant and the bread basket
      [70, 84],   // under the fountain base
      [80, 86],
      [84, 91],   // stop short of the bottom-right lavender
      [70, 95],
      [40, 97],   // bottom of the garden path
      [28, 95],
      [26, 91],   // clear of the roses
      [31, 88],   // below the raised bed
      [34, 85],
    ],

    /* The two signs, in image coordinates. On a wide screen they sit just OUTSIDE
     * the walkable polygon, on the grass beyond its left and right edges, so she
     * never stands on one.
     *
     * On a tall screen these positions are off-screen entirely — the scene is
     * cropped to fill and only a narrow band through the middle survives — so
     * ui.css docks the signs into a corner instead. See the aspect-ratio query
     * there; this data is only used on wide viewports. */
    signs: [
      { id: 'linkedin', ...signs.linkedin, x: 21, y: 90 },
      { id: 'email', ...signs.email, x: 88, y: 89 },
    ],

    /* Scenery signs: no link, not focusable, purely part of the painting. They live
     * in their own layer so they stay planted in the artwork even on a phone, where
     * the interactive signs dock into the corner instead. */
    decor: [
      { id: 'enter', ...decor.enter, x: 33, y: 86 },
    ],

    actors: [
      /* Jingwen is drawn in three pieces so her legs can actually swing. The numbers
       * are where the sprite was cut, as percentages of the whole sprite box — they
       * must match the cut or the hip seam shows. See assets/PROMPTS.md. */
      {
        id: 'jingwen', alt: actorAlt.jingwen,
        x: 46, y: 92, height: 30, facing: 1, walks: true,
        aspect: 222 / 720,
        parts: jingwenFront,
        poses: {
          front: jingwenFront,   // walking toward the viewer, or standing
          back: jingwenBack,     // walking away
          side: jingwenSide,     // walking left or right; drawn facing RIGHT and
                                 // mirrored with scaleX(-1) for the other way
        },
        bodyHeight: 63.194,
        legTop: 61.806,
        legHeight: 38.194,
      },
      { id: 'junnie', image: 'assets/sprites/junnie.png', alt: actorAlt.junnie,
        x: 57, y: 93, height: 15, facing: -1, walks: false, aspect: 237 / 420 },
    ],
  },

  // Phase 5. Declared so the router and renderer have something to fail against loudly
  // rather than silently rendering a blank stage.
  interior: {
    id: 'interior',
    image: null,
    alt: sceneAlt.interior,
    aspect: 1408 / 768,
    hasBackdrop: false,
    walkable: [],
    /* The two signs, in image coordinates. On a wide screen they sit just OUTSIDE
     * the walkable polygon, on the grass beyond its left and right edges, so she
     * never stands on one.
     *
     * On a tall screen these positions are off-screen entirely — the scene is
     * cropped to fill and only a narrow band through the middle survives — so
     * ui.css docks the signs into a corner instead. See the aspect-ratio query
     * there; this data is only used on wide viewports. */
    signs: [
      { id: 'linkedin', ...signs.linkedin, x: 21, y: 90 },
      { id: 'email', ...signs.email, x: 88, y: 89 },
    ],

    actors: [],
  },
};

export const DEFAULT_SCENE = 'exterior';

export function getScene(id) {
  return scenes[id] || null;
}
